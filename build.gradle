apply plugin: 'java'

defaultTasks 'jar'

group = 'ngdc.gis'
version = '2.0-SNAPSHOT'
archivesBaseName = 'dijits'

jar {
    from "."
    exclude 'README.md'
    exclude 'build*'
    exclude 'test'
    exclude '.gradle'
}
