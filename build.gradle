apply plugin: 'java'

defaultTasks 'clean', 'jar'

group = 'ngdc.gis'
version = '2.0-SNAPSHOT'
archivesBaseName = project.name

jar {
    from "."
    exclude 'README.md'
    exclude 'build*'
    exclude 'test'
    exclude '.gradle'
}
